# Legacy CLAUDE.md — Notes

The previous long CLAUDE.md files (root ≈1,724 lines, VinPoker ≈324 lines) were replaced by the
layered system (short root bootstrap + canonical `VinPoker/CLAUDE.md` + `docs/agent/*.md`).

The full original content was backed up **local-only (not committed)** at:

```
D:\Quy trình\backups\CLAUDE.root.legacy-20260619.md
D:\Quy trình\backups\CLAUDE.vinpoker.legacy-20260619.md
```

**Do not load the legacy instructions** unless the owner explicitly asks. They contain superseded,
duplicated, and internally contradictory rules (e.g. the old multi-agent contradiction) and may
reference stale paths/state — loading them would re-pollute context and re-introduce conflicts.

If a rule seems missing from the new docs, raise it with the owner rather than restoring the legacy
file wholesale.
