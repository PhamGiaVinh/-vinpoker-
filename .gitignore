.vercel
.mcp.json

# Env / secret files (defense in depth — never commit real values)
.env
.env.local
.env.*.local
.env.test
.env.secret
*.local

# Claude Code local/runtime config (keep agents/, hooks/, commands/, settings.json trackable)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/*.local.md

# Local-only backups (e.g. legacy CLAUDE.md snapshots) — never commit
backups/
